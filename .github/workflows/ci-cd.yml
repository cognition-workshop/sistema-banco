name: CI/CD Pipeline

on:
  push:
    branches: [ master, develop, 'feature/**' ]
  pull_request:
    branches: [ master, develop, 'feature/**' ]

jobs:
  lint:
    name: Code Linting
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 black
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
    
    - name: Run flake8
      run: flake8 . --count --show-source --statistics
    
    - name: Check black formatting
      run: black --check .

  test:
    name: Run Tests
    runs-on: ubuntu-latest
    needs: lint
    
    services:
      redis:
        image: redis:6
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run migrations
      run: |
        python manage.py migrate
    
    - name: Run Django tests
      run: |
        python manage.py test
      env:
        REDIS_URL: redis://localhost:6379

  deploy:
    name: Deploy (Placeholder)
    runs-on: ubuntu-latest
    needs: [lint, test]
    if: github.ref == 'refs/heads/master' || startsWith(github.ref, 'refs/heads/feature/time8')
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Deployment Placeholder
      run: |
        echo "==================================="
        echo "DEPLOYMENT CONFIGURATION REQUIRED"
        echo "==================================="
        echo ""
        echo "This is a placeholder deployment job."
        echo "To enable automated deployment, configure the following:"
        echo ""
        echo "1. DEPLOYMENT PLATFORM:"
        echo "   - Choose: Heroku, AWS, Docker, VPS, etc."
        echo "   - Add deployment credentials to GitHub Secrets"
        echo ""
        echo "2. DATABASE:"
        echo "   - Current: SQLite (not suitable for production)"
        echo "   - Recommended: PostgreSQL or MySQL"
        echo "   - Configure DATABASE_URL in secrets"
        echo ""
        echo "3. REDIS:"
        echo "   - Current: localhost:6379"
        echo "   - Production: Redis Cloud, AWS ElastiCache, etc."
        echo "   - Configure REDIS_URL in secrets"
        echo ""
        echo "4. ENVIRONMENT VARIABLES:"
        echo "   - SECRET_KEY (currently hardcoded)"
        echo "   - DEBUG=False for production"
        echo "   - ALLOWED_HOSTS (configure domain)"
        echo ""
        echo "5. KNOWN ISSUES TO FIX BEFORE PRODUCTION:"
        echo "   - Balance validation bug (transactions/forms.py:67-68)"
        echo "   - Authentication bypass (transactions/views.py:32-36)"
        echo "   - Hardcoded SECRET_KEY (banking_system/settings.py:23)"
        echo ""
        echo "==================================="
        echo "For deployment examples, see:"
        echo "- Heroku: https://devcenter.heroku.com/articles/django-app-configuration"
        echo "- Docker: https://docs.docker.com/samples/django/"
        echo "==================================="
